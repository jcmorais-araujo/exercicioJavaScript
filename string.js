var nome = 'Joao carlos'


document.write(nome.length); //retorna o comprimento

document.write(nome.charAt(5)); //retorna a posição que sugerir

document.write(nome.indexOf('l')) //retorna a primeira posição da letra que sugerir

document.write(nome.replace('Joao carlos', 'João Carlos'));

document.write(nome.substring(0,3)); //retorna os valores dentro do limite que desejo

document.write(nome.toLocaleLowerCase()) //retorna valores em caixa baixa;

document.write(nome.toUpperCase()); //retorna valores em caixa alta;
